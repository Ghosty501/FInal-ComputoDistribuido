apiVersion: v1
kind: Service
metadata:
  name: contacts-service
  namespace: crm
spec:
  type: ClusterIP
  selector:
    app: contacts
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
