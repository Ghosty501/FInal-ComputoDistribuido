apiVersion: v1
kind: Service
metadata:
  name: crm-db
  namespace: crm
spec:
  type: ClusterIP
  selector:
    app: crm-db
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
